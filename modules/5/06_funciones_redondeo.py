from math import *

x=1.2
y=3
ceil(x) # retorna el entero más pequeño mayor
floor(x) # retorna el entero más grande menor
trunc(x) # no es equivalente a ceil o floor, parece quitar los decimales y       
         #convertir a entero el número
factorial(y) # el factorial
hypot(x,y) # devuelve la longitud de la hipotenusa de un TR con
           # de los catetos iguales a x ey, lo mismo que 
           # sqrt(pow(x,2)+pow(y,2)) lo mismo pero más preciso
print(factorial(y))